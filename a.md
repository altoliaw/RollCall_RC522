# rollcall.descripotion.md 檢視紀錄

檢視日期：2026-08-01
檢視對象：`rollcall.descripotion.md`（STM32 藍芽 RFID 無線點名機專案說明）
比對依據：`RollCall_RC522.ioc`、`Core/Src/main.c`（目前 repo 中已由 CubeMX 產生的實際設定）

## 整體結論

文件描述的架構（RC522 → STM32F767ZI → HC-06 → PC/Python）方向可行，且腳位表、SPI1/USART3/TIM2 的
CubeMX 設定與目前專案的 `RollCall_RC522.ioc`、`main.c` **實際生成的設定一致**（PA4/PA5/PA6/PA7/PB6/PD8/PD9/PA15，
SPI1 Master、USART3 9600、TIM2 CH1 PWM，Prescaler 83／Period 1000）。屬於規劃文件 + 尚未實作的階段（Core 目錄
目前只有 CubeMX 骨架，尚無 RC522 驅動、蜂鳴器程式、Python 端程式碼）。

有一項**確定的技術錯誤**與兩項**需要驗證的可行性風險**，列於下方。

---

## 1. 確定錯誤：蜂鳴器 PWM 頻率計算的時脈假設與實際不符

- 文件第 94 行註解：「Prescaler (PSC): 83（若定時器時脈為 84MHz，將計數頻率除降為 1 MHz）」，
  範例程式碼中的音調計算也是以 **計數頻率 = 1 MHz** 為前提（例如 `1000000 / 1046`）。
- 但實際 `main.c` 的 `SystemClock_Config()` 是：
  - `RCC_OscInitStruct.PLL.PLLState = RCC_PLL_NONE;`（PLL 未啟用）
  - `SYSCLKSource = RCC_SYSCLKSOURCE_HSI`（16 MHz）
  - `APB1CLKDivider = RCC_HCLK_DIV1`（不分頻）
  - 因此 TIM2（掛在 APB1）實際時脈是 **16 MHz**，不是文件假設的 84 MHz。
- 在 PSC=83 的情況下，實際計數頻率為 `16MHz / (83+1) ≈ 190.5 kHz`，與程式碼假設的 1 MHz 相差約 5.25 倍，
  會導致 `__HAL_TIM_SET_AUTORELOAD` 算出的 ARR/CCR 值不正確，蜂鳴器實際發出的音高會與設計意圖（1046Hz「叮」、
  784Hz「咚」）完全不同（頻率會偏高數倍，可能超出人耳舒適範圍或無源蜂鳴器有效發聲範圍）。
- **建議修正方式（擇一）**：
  1. 在 CubeMX 中啟用 PLL，將 SYSCLK 拉高到常見的 F7 設定（如 96MHz/216MHz）並讓 APB1 timer clock 落在
     84MHz 附近，這樣文件的計算公式才成立；或
  2. 保留目前 16MHz HSI 設定，直接依實際時脈重新計算 PSC/ARR（例如 PSC 設為 15，計數頻率即為 1MHz，
     公式維持不變且更省電、更單純，不需要動 PLL）。
  - 選項 2 對此專案（低速點名應用，不需要高運算效能）更省事，建議優先採用。

---

## 2. 需要驗證：RC522（MFRC522）能否讀取台灣悠遊卡／一卡通

- MFRC522 晶片原生支援的是 ISO14443 Type A（如 Mifare Classic/Ultralight），文件 BOM 中直接寫「讀取學生證/
  悠遊卡/一卡通之 UID」。
- 悠遊卡與一卡通是否能被 RC522 穩定讀出 UID，市面上實測結果不完全一致（部分卡片可讀出 UID、部分因底層協定
  差異讀不到），建議在硬體到齊後，**優先用實際卡片跑一次讀卡測試**，而不是直接假設所有學生證/交通卡都相容。
  若讀不到，可能需要改用支援 FeliCa 模式的模組（如 PN532）。
- 這點文件目前完全沒有提到風險或備案，建議在文件中加一句「需以實際卡片驗證相容性」。

## 3. 需要驗證：4-byte UID 假設

- `Send_Card_ID_Via_Bluetooth()` 範例程式碼固定只組出 4 bytes（`uid[0..3]`）。多數 Mifare Classic/Ultralight
  卡片 UID 確實是 4 bytes，但也有 7-byte UID 的卡片型式存在。若之後遇到 7-byte 卡片，目前的格式化方式會截斷
  卡號，造成不同卡片被誤判為同一張卡（UID 碰撞）。建議在讀卡函式那一層先確認 UID 長度，再決定要送幾個 byte。

---

## 4. 文件目前尚未涵蓋、但落地時會需要的部分（非錯誤，屬於規格缺口）

- **卡號 ↔ 學生身分對照**：只提到 Python 端解析 `CARD:XXXXXX` 並「比對系統目前時間」寫入資料庫，但沒有描述
  UID 對應學生姓名/學號的資料表設計。
- **重複刷卡/去抖動邏輯**：同一張卡短時間內重複感應（例如卡片放在讀卡器上不拿開）如何避免寫入多筆重複紀錄，
  目前沒有規則描述（無論是韌體端或 Python 端）。
- **出席狀態判定規則**：「比對系統目前時間」具體如何轉換為「準時/遲到/曠課」，未定義規則或課程時段表。
- **HC-06 一對一限制**：HC-06 同時間只能配對一台裝置，若未來想要多台點名機同時連線同一台電腦，需要多個藍芽
  接收端或改用支援多連線的模組，目前文件未提及此限制（目前單一終端機場景下不影響可行性）。
- **例外處理**：藍芽斷線重連、UART 資料半包/亂碼、資料庫寫入失敗等情境，目前 Python 段落只有兩句話帶過，
  尚未有實作層級的設計。

---

## 5. 已核對且沒有問題的部分

- RC522 3.3V / STM32 5V 供電區分、TX/RX 交叉接線的提醒正確且必要。
- SPI1 腳位（PA4/PA5/PA6/PA7/PB6）、USART3 腳位（PD8/PD9）、TIM2_CH1（PA15）與 `.ioc` 內容完全一致。
- SPI1 BaudRatePrescaler=4 對應到目前 16MHz APB2 時脈，算出來是 4MHz，落在文件要求的「4~8MHz」範圍內，沒有問題
  （這點恰好與 TIM2 的情況不同，SPI 沒有踩雷）。
- USART3 9600 bps 為 HC-06 出廠預設值，且 `HAL_UART_Init` 是依實際時脈自動換算 BRR，不受 SYSCLK 變更影響，正確。
- PA15 作為 TIM2_CH1 使用：此專案未啟用完整 JTAG（僅 SWD），不會與除錯腳位衝突，可行。

---

## 總結建議優先順序

1. **必修**：先修正 TIM2 的 PSC（或改接 PLL），否則蜂鳴器聲音會是錯的音高。
2. **必測**：硬體到齊後第一件事就是拿實際的悠遊卡/一卡通/學生證測試 RC522 讀卡是否成功。
3. **建議補強**：UID 長度判斷、卡號對應學生資料表設計、重複刷卡去抖動、出席規則，這些在寫 Python/資料庫那一段
   程式碼前需要先定義清楚，目前文件停留在「概念可行」層級，還不到可以直接照抄實作的程度。
